import React, { Component } from "react";

export default class SearchBar extends Component {
  state = {
    searchTerm: ""
  };
  render() {
    return <h1>This is a serach bar component</h1>;
  }
}
